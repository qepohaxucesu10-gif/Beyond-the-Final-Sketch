# Evaluation

The project evaluation reports F1, IoU, L1, and EdgeLoss. The included CSV is a reported pix2pix per-subject metric asset. Released evaluation scripts and split files should be used with the frozen cohorts; canonical Test42 is not used for checkpoint selection.
