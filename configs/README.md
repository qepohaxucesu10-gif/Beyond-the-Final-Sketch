# Frozen configurations

This directory will contain the released configurations for Direct, Sparse B7/B19, Full25, Ordered, Shuffled, the preliminary cascade, reduced diffusion exploration, and the endpoint-only pix2pix reference. Paths and release-specific data identifiers are intentionally not hard-coded in this staging repository.
